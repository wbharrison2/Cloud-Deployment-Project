# DO NOT commit real credentials here.
# Create the secret with kubectl:
#
#   kubectl create secret generic agw-secrets -n agw-production \
#     --from-literal=JWT_SECRET="$(openssl rand -base64 64)" \
#     --from-literal=DATABASE_URL="postgres://agw:PASS@RDS_ENDPOINT:5432/agwdb?sslmode=require" \
#     --from-literal=REDIS_URL="rediss://:PASS@ELASTICACHE_ENDPOINT:6379" \
#     --from-literal=STRIPE_SECRET_KEY="sk_live_..." \
#     --from-literal=CLOUDFRONT_DISTRIBUTION_ID="E..."
#
# This template is a placeholder that shows the expected shape.
# Replace CHANGE_ME values before applying.
apiVersion: v1
kind: Secret
metadata:
  name: agw-secrets
  namespace: agw-production
type: Opaque
data:
  # All values must be base64-encoded: echo -n 'value' | base64
  JWT_SECRET: Q0hBTkdFX01F
  DATABASE_URL: Q0hBTkdFX01F
  REDIS_URL: Q0hBTkdFX01F
  STRIPE_SECRET_KEY: Q0hBTkdFX01F
  CLOUDFRONT_DISTRIBUTION_ID: Q0hBTkdFX01F
